# Pinned to the versions verified against the v1 parity run (docs/CHANGES.md).
# pandas 3 changes text/NaN semantics: re-run tools/parity.py before upgrading.
pandas==2.3.3
numpy==2.0.2
openpyxl==3.1.5
python-calamine==0.4.0
pyarrow==21.0.0
streamlit==1.50.0
altair==5.5.0
gspread==6.2.1
google-auth==2.50.0
requests==2.32.5
